# RYM_VARGANI V1.7.2.6 — Dashboard + Reports + Day-wise Layout

- Day-wise Activity columns are now:
  Receipt → Donor → Mode of Payment → Amount → Receipt Date → Payment Received Date → Status.
- Day-wise Activity switches to clean stacked cards on mobile.
- Donor Collection Report is collapsed by default and uses **Click to expand / Click to collapse**.
- Excel download remains visible only to authorized roles and appears inside the expanded report.
- Dashboard **Donors** renamed to **Total Donors**.
- Dashboard **Pending Donors** renamed to **Pending Receipts**.
- Dashboard adds **Receipt Given - Payment Pending** showing the total amount currently in that status.
- No new Supabase migration required.
