# RYM_VARGANI V1.6.9.3 — Unified Receipt Renderer

- Fixed the mismatch where the on-screen View was calibrated but Print/PDF and WhatsApp still used older canvas coordinates.
- Export renderer now mirrors the final V1.6.9.2 preview coordinates for receipt number, date, donor name and numeric amount.
- The date placeholder area is fully masked before the clean generated date is drawn.
- Print/PDF and WhatsApp both consume the same prepared receipt image generated by `createReceiptImage()`.
- No Supabase SQL upgrade is required.
